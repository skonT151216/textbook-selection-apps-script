# 한눈에 보는 Google Apps Script 운영 매뉴얼

## 선생님께 안내할 내용 — 3단계

선생님은 Apps Script 편집기나 Google 스프레드시트를 열 필요가 없습니다.

1. 업무담당자가 보낸 **교과용도서 선정 웹 앱 주소**를 누릅니다.
2. 담당자가 따로 알려 준 **학교 아이디**를 입력하고 **내 업무 시작하기**를 누릅니다.
3. 본인 이름과 담당 교과가 맞는지 확인한 뒤 평가를 작성합니다. 작업을 마치면 오른쪽 위 **로그아웃**을 누릅니다.

> Google 계정 로그인, 비밀번호, 이메일 인증은 없습니다. 다른 선생님의 아이디로 접속하지 말고, 주소와 아이디를 학교 밖에 공유하지 않습니다.

아이디 자체가 접근 수단입니다. 아이디를 아는 사람은 그 사람으로 접속할 수 있으므로 이름·사번처럼 추측하기 쉬운 값 대신 충분히 길고 임의적인 값을 발급하고 개별 전달합니다. 이 방식은 강한 본인 인증을 제공하지 않습니다. 학생 개인정보 등 민감한 자료를 입력하지 마세요.

즐겨찾기는 배포 주소 끝이 `/exec`인 페이지로 등록합니다. `/dev` 주소는 담당자 테스트용이므로 선생님에게 배포하지 않습니다.

## 업무담당자 — 최초 1회 설치

### 1. 배포 파일 준비

배포 압축파일을 받은 담당자는 압축만 풀면 됩니다. 아래 명령은 소스에서 배포본을 다시 만드는 개발자용입니다.

```bash
npm ci
npm run build:gas
npm test
```

배포 파일은 `Code.gs`, `Index.html`, `appsscript.json` 세 개입니다. 화면 파일은 `Index.html` 하나입니다.

압축파일의 매뉴얼·PDF 예시·`THIRD_PARTY_LICENSES.md`는 참고용입니다. Apps Script에는 위 세 파일만 넣습니다. 프로그램을 재배포할 때는 함께 제공한 외부 라이브러리 저작권 고지도 보존합니다.

`Index.html`은 브라우저로 열지 말고 메모장 또는 코드 편집기로 열어 전체 내용을 복사합니다. 영문·숫자로 된 긴 내용은 정상이며 직접 수정하지 않습니다. 실행할 때 프로그램이 화면과 매뉴얼 이미지로 복원합니다.

### 2. 학교용 스프레드시트와 스크립트 만들기

1. Google Drive에서 빈 스프레드시트를 하나 만듭니다. 예: `2027 교과용도서 선정 데이터`.
2. **확장 프로그램 → Apps Script**를 누릅니다.
3. 기본 `Code.gs`에서 `Ctrl+A`로 기존 내용을 모두 선택한 뒤, 생성된 `apps-script/Code.gs` 내용으로 완전히 바꿉니다.
4. 왼쪽 **+ → HTML**로 이름이 `Index`인 파일을 만듭니다. 이미 있으면 그 파일을 엽니다. 편집기 안에서 `Ctrl+A`로 모두 선택하고 새 `Index.html` 내용으로 교체합니다.
5. **프로젝트 설정 → 편집기에 appsscript.json 매니페스트 파일 표시**를 켭니다.
6. `appsscript.json` 내용을 생성된 파일 내용으로 바꾸고 모두 저장합니다.

저장이 끝난 뒤 `Code.gs`를 선택하고 상단 함수 목록에서 `verifyInstallation` → **실행**을 누르면 파일 복원 상태를 검사할 수 있습니다. 실행 로그에 `"complete":true`와 `"error":""`가 나오면 파일 복원이 정상입니다. 이 검사는 학교 설정과 데이터를 변경하지 않습니다. 이후 웹 앱을 열어 실제 화면도 확인합니다.

> 기존 사용자는 같은 압축본의 `Code.gs`와 `Index.html`을 함께 교체합니다. 이전 `AppHtml01`~`AppHtml07`은 사용하지 않으므로 새로 만들 필요가 없습니다. 이미 있다면 남겨 두어도 됩니다. 학교 설정을 다시 하거나 데이터 시트를 지울 필요는 없습니다.

### 3. 학교명과 최초 아이디 설정

1. 스프레드시트로 돌아가 새로고침합니다.
2. 상단 **교과서 선정 프로그램 → 최초 학교 설정**을 누릅니다.
3. 학교명과 최초 업무담당자 아이디를 입력합니다. 예: `manager`.
4. 처음 한 번 Google 권한 승인 화면이 나오면 배포 담당자 계정으로 승인합니다.

설정 뒤 생기는 `_APP_DATA` 숨김 시트는 삭제하거나 직접 수정하지 않습니다.

### 4. 웹 앱 배포

1. Apps Script 오른쪽 위 **배포 → 새 배포**를 누릅니다.
2. 유형에서 **웹 앱**을 선택합니다.
3. 실행 사용자는 **나**, 액세스 권한은 **모든 사용자**로 정합니다.
4. **배포**를 누르고 `/exec`로 끝나는 웹 앱 URL을 복사합니다.

조직 정책 때문에 **모든 사용자**를 선택할 수 없다면 Google Workspace 관리자에게 Apps Script 웹 앱의 익명 접근 허용 여부를 확인해야 합니다. 이 옵션이 없으면 현재의 ‘Google 로그인 없는 아이디 방식’으로 교사 공동 사용을 배포할 수 없습니다.

### 5. 교직원 아이디 발급

1. `/exec` 주소를 열고 최초 담당자 아이디로 로그인합니다.
2. 초기 설정에서 학교·도서·협의회 정보를 입력합니다.
3. 담당자·교감·교사별로 겹치지 않는 학교 아이디를 정합니다.
4. 역할과 이름을 최종 확인한 뒤 설정을 저장합니다.
5. 각 선생님에게 **웹 앱 주소 + 본인 아이디**만 개별 안내합니다.

## 업데이트 방법

1. 새 배포 압축파일을 풀어 준비합니다. 개발자는 `npm run build:gas`, `npm test`로 배포본을 생성·검증합니다.
2. Apps Script의 `Code.gs`, `Index.html`을 같은 압축본의 내용으로 교체하고 저장합니다. `appsscript.json`은 기존 내용과 다를 때만 교체합니다.
3. **배포 → 배포 관리 → 수정 → 새 버전 → 배포**를 누릅니다.
4. 기존 `/exec` 주소에서 담당자 로그인, 교사 로그인, 저장, PDF 다운로드를 확인합니다.

기존 배포를 수정하면 `/exec` 주소는 보통 그대로 유지됩니다. **새 배포**를 별도로 만들면 주소가 달라질 수 있으므로 교직원에게 안내한 링크를 확인합니다.

## PDF 예시 확인 — 실제 평가와 분리

1. 담당자 로그인 후 **서식 테스트**를 엽니다.
2. **서식 1·2·3 예시 자동 생성**을 누릅니다.
3. 미리보기에서 끝까지 아래로 내려 모든 페이지를 확인합니다. 교사 2명 기준 서식1 두 장, 서식2 한 장, 서식3 한 장으로 총 4쪽입니다.
4. **PDF 다운로드**로 저장한 파일도 열어 확인합니다.

예시에는 가상 이름·점수·의견·TEST 서명과 ‘테스트용·제출 불가’ 표시가 있습니다. 공식 제출 자료로 사용하지 않습니다. 미리보기는 PDF에 삽입되는 이미지와 동일한 배치로 표시하고, 다운로드는 실제 PDF 파일로 제공합니다. 긴 의견·많은 도서 등 학교별 실제 내용은 제출 전에 다시 확인합니다.

## 배포 직후 점검표

- [ ] 등록되지 않은 아이디가 거부된다.
- [ ] 교사 아이디로 본인 교과와 본인 평가만 보인다.
- [ ] 교과부장만 평가기준을 수정할 수 있다.
- [ ] 교감은 추천의견 최종 확인만 할 수 있다.
- [ ] 저장 후 새로고침해도 내용이 남는다.
- [ ] 서식 1·2·3 미리보기와 PDF 다운로드가 된다.
- [ ] 로그아웃 뒤 뒤로가기로 업무 화면이 다시 열리지 않는다.
- [ ] `/exec` 주소와 아이디 목록을 학교 내부에서만 공유했다.

## 자주 생기는 문제

| 화면/증상 | 확인할 것 |
|---|---|
| 최초 설정 안내만 보임 | 스프레드시트 메뉴의 **최초 학교 설정**을 실행했는지 확인 |
| 등록되지 않은 아이디 | 띄어쓰기, 철자, 담당자가 저장한 최신 아이디 확인 |
| 권한 승인 화면 | 최초 설치 담당자만 승인하며 일반 교사는 승인하지 않음 |
| 변경 내용이 안 보임 | 기존 배포에 **새 버전**을 반영했는지 확인 |
| 설치 파일 확인 안내가 뜸 | 같은 압축본의 `Code.gs`와 `Index.html` 전체 내용을 교체하고 저장한 뒤 새 버전 배포 |
| 제목만 있고 화면이 흰색임 | 최신 압축본의 Code.gs와 Index.html을 함께 교체하고 새 버전 배포. 이전 화면 코드는 HTML 처리 중 잘릴 수 있으므로 기존 Index를 그대로 재사용하지 않음 |
| PDF 미리보기에 회색 빈 화면만 보임 | 2026-09-19 수정본 이상의 Code.gs·Index.html을 함께 반영하고 새 버전 배포. 파일 다운로드 후 별도 PDF 뷰어에서도 확인 |
| 저장 충돌 안내 | 다른 사용자가 먼저 저장했으므로 새로고침 후 다시 입력 |
| 모든 사용자 선택 불가 | 학교 Google Workspace의 Apps Script 외부/익명 웹 앱 정책 확인 |
| `Unexpected identifier 'xmlns'` | 이전 배포본의 `Code.gs`를 최신 파일로 교체하고 기존 웹 앱에 **새 버전** 배포 |
